services:
  - type: web
    name: youtube-downloader
    runtime: node
    plan: free
    buildCommand: |
      npm install
      pip install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
